<Page
    x:Class="ERP.Pages.TablePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ERP.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:vm="using:ERP.ViewModels"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <vm:TableViewModel/>
    </Page.DataContext>

    <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" Padding="10">
        <TextBlock x:Name="Title" Style="{StaticResource TitleTextBlockStyle}" TextAlignment="Center"/>

    </StackPanel>
</Page>
